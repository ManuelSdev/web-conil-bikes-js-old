import React from 'react'

export default function AuthActionsPageHandler({ searchParams }) {
   const { mode, oobCode: actionCode, apiKey, continueUrl, lang } = searchParams

   return <div>AuthActionsPageHandler</div>
}
